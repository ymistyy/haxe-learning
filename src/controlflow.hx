// Control flow If / Else.

class Main {
    static function main() {
        var age = 20;
        if (age >= 20) {
            trace ("old");
        } else {
            trace ("young"); 
        }
    }
}
